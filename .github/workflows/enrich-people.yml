name: Enrich People Data

on:
  workflow_dispatch:
    inputs:
      batch_size:
        description: 'Number of people to process (default: 200)'
        required: false
        default: '200'
      start_from_id:
        description: 'Optional: Start from specific person ID'
        required: false
        default: ''
      dry_run:
        description: 'Dry run mode (no database updates)'
        required: false
        type: boolean
        default: false

jobs:
  enrich-people:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run People Enrichment
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          TMDB_ACCESS_TOKEN: ${{ secrets.TMDB_ACCESS_TOKEN }}
          WIKI_USER_AGENT: ${{ secrets.WIKI_USER_AGENT }}
          BATCH_SIZE: ${{ github.event.inputs.batch_size || '200' }}
          START_FROM_ID: ${{ github.event.inputs.start_from_id }}
          DRY_RUN: ${{ github.event.inputs.dry_run || 'false' }}
        run: npx tsx scripts/enrich-people.ts

      - name: Summary
        if: always()
        run: |
          echo "## 👥 People Enrichment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Batch Size**: ${{ github.event.inputs.batch_size || 200 }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Dry Run**: ${{ github.event.inputs.dry_run || false }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Run again to process the next batch. Script resumes automatically." >> $GITHUB_STEP_SUMMARY
